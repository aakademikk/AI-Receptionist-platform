node_modules/
dist/
.next/
out/
build/
coverage/
*.tsbuildinfo

.env
.env.local
.env.*.local
!.env.example

# Local environment backups — never commit these. The repo is public, so a
# committed backup is world-readable, not just local. Covers the
# .env.local.bak-<date> files the existing patterns above do not match.
.env*.bak
.env*.bak-*

.vercel
.turbo
.supabase/
supabase/.branches/
supabase/.temp/

n8n-data/
postgres-data/

.DS_Store
*.log
npm-debug.log*
pnpm-debug.log*

# Next.js build output
apps/web/.next/
apps/web/next-env.d.ts
